import math 


def calculate_distance(p1, p2): 
    
    return math.sqrt(math.pow(p2.x - p1.x,2) +  math.pow(p2.y - p1.y,2) )

def get_min_distances(positions): # positions = [[x,y] , [x,y]]

    #for i in range(len(positions)):
    return 0
